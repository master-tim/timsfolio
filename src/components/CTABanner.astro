---
---

<section class="py-20 reveal-on-scroll opacity-0" style="transform: translateY(30px);">
  <div class="bg-gradient-to-r from-pblue via-ppink to-pyellow border-3 border-black rounded-3xl shadow-hover p-12 text-center relative overflow-hidden">
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-0 left-0 w-40 h-40 bg-pgreen rounded-full -translate-x-1/2 -translate-y-1/2"></div>
      <div class="absolute bottom-0 right-0 w-60 h-60 bg-ppink rounded-full translate-x-1/3 translate-y-1/3"></div>
    </div>
    
    <div class="relative z-10">
      <h2 class="text-5xl font-bold mb-4">Let's Build Something Amazing Together</h2>
      <p class="text-xl mb-8 max-w-2xl mx-auto">
        I'm always excited to work on innovative projects that push the boundaries of web technology, 3D graphics, and AI.
      </p>
      <div class="flex gap-4 justify-center flex-wrap">
        <a
          href="/about"
          class="bg-black text-white px-8 py-4 border-3 border-black rounded-full shadow-card hover:shadow-hover transition hover:scale-105 font-bold"
        >
          View My Work
        </a>
        <a
          href="mailto:your.email@example.com"
          class="bg-pgreen px-8 py-4 border-3 border-black rounded-full shadow-card hover:shadow-hover transition hover:scale-105 font-bold"
        >
          Get In Touch
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Add subtle pulse animation */
  .reveal-on-scroll.visible {
    animation: pulse-glow 3s ease-in-out infinite;
  }

  @keyframes pulse-glow {
    0%, 100% {
      filter: brightness(1);
    }
    50% {
      filter: brightness(1.05);
    }
  }
</style>
